# Copy to .env and paste your Trustless Work TESTNET API key (from https://dapp.trustlesswork.com).
# Never commit .env. Read-only calls work without a key; write flows (deploy/fund/release) require it.
VITE_TW_API_KEY=
